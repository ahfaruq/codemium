description = "Run Codemium with optional fast, deep, or critical engineering depth."
prompt = """
Use the `cm` Codemium Agent Skill for this coding request:

{{args}}

Interpret a leading `fast`, `deep`, or `critical` as the requested engineering-depth override. Otherwise use automatic depth. Apply the Codemium safety floor and shared `.codemium/` project intelligence. Do not expand this command into duplicate always-on doctrine; load detailed behavior from the `cm` skill when needed.
"""
