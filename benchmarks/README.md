# Codemium Benchmarks

The included example is synthetic and demonstrates the input schema only. Do not present it as product performance. Real comparisons should run identical tasks/repository states across Vanilla Codex, Ponytail, Codex Optima, and Codemium, with correctness/testing/security quality gates evaluated before efficiency metrics.
