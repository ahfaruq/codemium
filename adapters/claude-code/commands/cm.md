---
name: cm
description: Run Codemium on a coding task with optional fast, deep, or critical depth.
argument-hint: "[fast|deep|critical] <coding task>"
---

Use the `cm` Codemium skill for the following request:

$ARGUMENTS

Interpret a leading `fast`, `deep`, or `critical` as a requested engineering-depth override. Otherwise use automatic depth. Apply the Codemium safety floor, Project Brain reuse, bounded working set, smallest justified change, risk-aware verification, scope guard, and explicit stop condition.
